version: '3'
services:
  template_nginx:
    networks: 
      company_template_nginx_staging:
        aliases:
          - templatecinginx
networks:
  company_template_nginx_staging:
    external: true