version: '3.7'
services:
  nginx:
    networks: 
      company_templatez_network_nginx_staging:
        aliases:
          - nginxstaging
networks:
  company_templatez_network_nginx_staging:
    external: true