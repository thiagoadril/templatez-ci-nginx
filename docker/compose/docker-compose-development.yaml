version: '3'
services:
  template_nginx:
    networks: 
      company_template_nginx_development:
        aliases:
          - templatecinginx
networks:
  company_template_nginx_development:
    external: true