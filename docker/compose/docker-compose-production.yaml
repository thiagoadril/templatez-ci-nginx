version: '3.7'
services:
  nginx:
    networks: 
      company_templatez_network_nginx_production:
        aliases:
          - nginxproduction
    links:
      - "frontenddevelopment"
networks:
  company_templatez_network_nginx_production:
    external: true